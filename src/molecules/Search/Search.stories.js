import React from "react";

import Search from "./Search";

export default {
  title: "2 - Molecules/Search",
  component: Search,
};

const Template = (args) => <Search {...args} />;

export const Primary = Template.bind({});
